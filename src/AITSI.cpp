#include <iostream>
#include <fstream>
#include <list>
using namespace std;
#include "parser.h"
#include "types.h"
#include "syntax.h"

int main(string args[], int argc) {
	if (argc == 0) {
		cout << "No arguments, to see help use \"help\" parameter";
		return 0;
	}
	if (args[0] == "-help")
		cout << "Usage...";
	break;
}

return 0;
}
